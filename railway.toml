[build]
builder = "nixpacks"

[deploy]
startCommand = "bash -c 'cd /app/backend && /opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
