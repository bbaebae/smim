providers = ["python"]

[phases.install]
cmds = [
  "python3 -m venv --copies /opt/venv",
  "/opt/venv/bin/pip install -r backend/requirements.txt"
]

[start]
cmd = "bash -c 'cd /app/backend && /opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
